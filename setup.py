from setuptools import setup, find_packages

setup(
    name="tile-matcher",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your project's dependencies here
        # They can be found in backend/requirements.txt
    ],
    python_requires='>=3.8',
)
